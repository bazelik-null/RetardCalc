// Introduction to Functions
// To execute use ./Morsel examples/functions.msl
// Or ./Morsel and "file examples/functions.msl"

// You can provide explicit type annotation
fn add(x: int, y: int): int {
    x + y
}

fn greet(name: string) {
    std::io::println("Hello,", name, "!");
}

fn max_of_three(a: int, b: int, c: int) {
    std::math::max(a, b, c)
}

fn rectangle_area(length: int, width: int) {
    length * width
}

fn celsius_to_fahrenheit(celsius: float) {
    (celsius * 9.0 / 5.0) + 32.0
}

fn main() {
    std::io::println("== What is a Function? ==");
    std::io::println("A function holds a piece of code that does a specific task.");
    std::io::println("A function takes some data as input, the code inside the function does something with the data, and then the result is returned.\n");

    std::io::println("== Simple Functions ==");
    let result1 = add(5, 3);
    std::io::println("5 + 3 =", result1);

    let result2 = add(10, 20);
    std::io::println("10 + 20 =", result2);

    std::io::println("\n== Functions Make Code Reusable ==");
    greet("Alice");
    greet("Bob");
    greet("Charlie");

    std::io::println("\n== Functions with Multiple Parameters ==");
    let largest = max_of_three(15, 42, 8);
    std::io::println("Largest of 15, 42, 8 is:", largest);

    std::io::println("\n== Functions that Return Values ==");
    let area = rectangle_area(10, 5);
    std::io::println("Area of 10 x 5 rectangle:", area);
    std::io::println("Area of 7 x 3 rectangle:", rectangle_area(7, 3));

    std::io::println("\n== A Practical Example ==");
    let temp_c = 25.0;
    let temp_f = celsius_to_fahrenheit(temp_c);
    std::io::println(temp_c, "°C is", temp_f, "°F");

    let another_temp = celsius_to_fahrenheit(0.0);
    std::io::println("0°C is", another_temp, "°F");

    std::io::println("\n== The main() Function ==");
    std::io::println("Every program needs a main() function. That's where your program starts.");
}
