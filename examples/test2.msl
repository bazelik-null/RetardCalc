// Test file

func main() {
    let x = 5;

    let message = is_positive(x);
    print(message);
}

func print(message: string) {
    // Placeholder
}

func is_positive(x: int): string {
    let message = if (x > 0) {
        "positive"
    } else {
        "non-positive"
    }

    message
}